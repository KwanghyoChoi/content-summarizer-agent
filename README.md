# Content Summarizer

AI 기반 콘텐츠 요약 시스템 - YouTube 영상, PDF 문서, 웹페이지에서 정확한 원문 추출 후 신뢰할 수 있는 요약 노트 생성

## 특징

- 📝 **4가지 노트 형식**: 자세한 노트, 핵심 노트, 쉬운 노트, 마인드맵
- 🎯 **정확성 우선**: 원문에 있는 내용만 포함, 추론/추가 금지
- 🔍 **출처 추적**: 모든 내용에 타임스탬프/페이지 번호 표기
- 🤖 **자동 생성**: Anthropic API를 통한 완전 자동화
- 📋 **수동 모드**: API 없이도 프롬프트 생성 가능
- 💾 **원문 보존**: 요약과 함께 전체 원문 저장

## 빠른 시작

### 설치
```bash
pip install -r requirements.txt

# API 키 설정 (자동 생성 기능 사용시)
export ANTHROPIC_API_KEY='your-api-key-here'
```

### 사용법
```bash
# YouTube 요약 (가장 간단)
python summarize.py "https://youtube.com/watch?v=..." --auto

# PDF 요약
python summarize.py "./document.pdf" --auto

# 웹페이지 요약
python summarize.py "https://example.com/article" --auto
```

## 핵심 원칙

### 1. 정확성 우선
- **원문에 있는 내용만** 요약에 포함
- 추론, 추가, 확장 절대 금지
- 불확실한 내용은 "원문에서 명확하지 않음"으로 표기

### 2. 출처 추적 필수
- YouTube: 타임스탬프 `[HH:MM:SS]` 표기
- PDF: 페이지 번호 `[p.N]` 표기
- 웹페이지: 섹션/헤딩 참조 표기

### 3. 원문 보존
- 모든 노트에 원문 스크립트/텍스트 전문 첨부
- 요약과 원문을 항상 함께 제공

## 출력 형식

### 자세한 노트 (detailed)
전체 내용을 계층적 구조로 정리
- 계층 구조: 1. → 1.1 → 1.1.1
- 모든 예시, 구체적 설명 유지
- 각 항목에 타임스탬프/페이지 표기

### 핵심 노트 (essence)
주요 개념만 추출
- 5-10개 핵심 포인트
- 개념 간 관계 설명
- 중요도 순으로 정리

### 쉬운 노트 (easy)
입문자도 이해할 수 있게
- 3-5개 최핵심만
- 전문용어 풀어서 설명
- 비유와 예시 활용

### 마인드맵 (mindmap)
시각적 구조 파악
- Mermaid mindmap 문법
- 중심 주제 → 주요 가지 → 세부 가지
- 텍스트 트리도 함께 제공

## 사용 예시

### 방법 1: 올인원 (추천)
```bash
# 자동으로 소스 타입 감지 + 추출 + 노트 생성
python summarize.py "URL 또는 파일경로" --auto

# 특정 형식만
python summarize.py "URL" --formats essence,mindmap --auto
```

### 방법 2: 단계별
```bash
# 1단계: 추출
python main.py --youtube "URL" --extract-only

# 2단계: 노트 생성
python generators/note_generator.py output/youtube_*_raw.md --all --auto
```

### 방법 3: 대화형
```bash
# 파일 선택과 형식 선택을 대화형으로
python quick_note.py
```

### 방법 4: API 없이 사용
```bash
# 프롬프트만 생성
python main.py --youtube "URL" --save-prompts

# 생성된 프롬프트를 Claude.ai에 복사
cat output/youtube_*_detailed_prompt.txt
```

## 워크플로우

```
1. 추출 (extract)
   ↓
   원본 텍스트 + 메타데이터 + 위치 정보
   ↓
2. 검증 (validate)
   ↓
   추출 품질 점수 (0-100) + 경고 사항
   ↓
3. 구조화 (structure)
   ↓
   섹션 분절 + 토픽 분류
   ↓
4. 노트 생성 (generate)
   ↓
   4가지 형식 동시 생성
```

## 프로젝트 구조

```
content-summarizer/
├── main.py                # 메인 실행 파일
├── summarize.py           # 올인원 래퍼
├── quick_note.py          # 대화형 노트 생성
│
├── extractors/            # 추출기
│   ├── youtube.py         # YouTube 자막 추출
│   ├── pdf.py             # PDF 텍스트 추출
│   └── web.py             # 웹페이지 본문 추출
│
├── generators/            # 노트 생성기
│   └── note_generator.py  # AI 노트 생성
│
├── templates/             # 노트 템플릿
│   ├── detailed.md        # 자세한 노트 템플릿
│   ├── essence.md         # 핵심 노트 템플릿
│   ├── easy.md            # 쉬운 노트 템플릿
│   └── mindmap.md         # 마인드맵 템플릿
│
└── output/                # 생성된 노트 저장
```

## 고급 기능

### 품질 검증
모든 추출에 품질 점수(0-100) 부여
- 자막 유형 (수동/자동)
- 텍스트 완성도
- 경고 사항 자동 표시

### 다중 언어 지원
```bash
# 영어 자막 선택
python main.py --youtube "URL" --lang en
```

### 출력 디렉토리 지정
```bash
python main.py --youtube "URL" --output-dir ./my-notes
```

### 프롬프트 커스터마이징
`templates/` 폴더의 프롬프트를 수정하여 자신만의 노트 스타일 정의 가능

## 환경 요구사항

### 필수
- Python 3.8+
- youtube-transcript-api (YouTube용)
- pdfplumber (PDF용)
- trafilatura (웹페이지용)

### 선택적
- anthropic (API 자동 생성용)
- pymupdf + pytesseract (스캔 PDF OCR용)

## 문서

- [USAGE.md](./USAGE.md) - 상세 사용 가이드
- [CLAUDE.md](./CLAUDE.md) - 개발자용 아키텍처 문서
- [workflow.md](./workflow.md) - Claude Code 프롬프트 예시

## 품질 체크리스트

노트 생성 후 반드시 확인:
- [ ] 모든 핵심 내용이 포함되었는가?
- [ ] 원문에 없는 내용이 추가되지 않았는가?
- [ ] 타임스탬프/페이지 번호가 정확한가?
- [ ] 원문 전문이 첨부되었는가?

## 라이선스

MIT License

## 기여

이슈와 Pull Request를 환영합니다!
